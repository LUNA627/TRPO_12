<Window x:Class="EF_Core.GroopAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EF_Core"
        mc:Ignorable="d"
        Title="GroopAddWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="2*" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Border Grid.Column="1" Grid.Row="1" BorderBrush="Black" BorderThickness="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.ColumnSpan="2" Text="Добавление" TextAlignment="Center" VerticalAlignment="Center" FontSize="24"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="id" Margin="34,0,0,13" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>

                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBox Width="300" IsReadOnly="True"/>
                </StackPanel>

                <TextBlock Grid.Row="2" Grid.Column="0" Margin="15,0,0,12" Text="Назание" HorizontalAlignment="Left" VerticalAlignment="Bottom" />

                <StackPanel Grid.Row="2" Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Width="300" />
                </StackPanel>

                <TextBlock Text="Описание" Grid.Row="3" Grid.Column="0"  Margin="10, 0, 0, 0" HorizontalAlignment="Center" VerticalAlignment="Center"/>


                <StackPanel Grid.Row="3" Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="300" />
                </StackPanel>

                <StackPanel Grid.Row="4" Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <Button Content="Добавить" Style="{StaticResource PrimaryButton}" Click="btnAdd_Click"/>
                    <Button Content="Назад" Style="{StaticResource PrimaryButton}" Click="btnBack_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
