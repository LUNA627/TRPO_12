<Page x:Class="EF_Core.UpdatePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EF_Core"
      xmlns:validators="clr-namespace:EF_Core.Validation"
      mc:Ignorable="d" 
      Background="White"
      d:DesignHeight="650" d:DesignWidth="800"
      Title="UpdatePage">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>


        </Grid.RowDefinitions>

        <TextBlock Text="ID:" FontWeight="Bold"  Style="{StaticResource LabelBlock}"/>
        <TextBox Grid.Row="1" Text="{Binding Id}" IsReadOnly="True" Style="{StaticResource InputTextBox}"/>

        <TextBlock Grid.Row="2" Text="Логин:" Style="{StaticResource LabelBlock}"/>
        <TextBox Grid.Row="3" Name="LoginTextBox" Style="{StaticResource InputTextBox}">
            <TextBox.Text>
                <Binding Path="Login" UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <validators:IsEmptyLoginRole/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>

        <TextBlock Grid.Row="5" Text="Имя:" Style="{StaticResource LabelBlock}"/>
        <TextBox Grid.Row="6" Name="NameTextBox" Style="{StaticResource InputTextBox}">
            <TextBox.Text>
                <Binding Path="Name" UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <validators:IsEmptyRole/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
       

        <TextBlock Grid.Row="8" Text="Email:" Style="{StaticResource LabelBlock}"/>
        <TextBox Grid.Row="9" Name="EmailTextBox" Style="{StaticResource InputTextBox}">
            <TextBox.Text>
                <Binding Path="Email" UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <validators:IsEmptyEmailRole/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
        

        <TextBlock Grid.Row="11" Text="Пароль:" Style="{StaticResource LabelBlock}"/>
        <TextBox Grid.Row="12" Name="PasswordTextBox" Style="{StaticResource InputTextBox}">
            <TextBox.Text>
                <Binding Path="Password" UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <validators:IsEmptyPassRole/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
       


        <StackPanel Grid.Row="15" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Сохранить" Style="{StaticResource PrimaryButton}" Click="OnSaveClick" Width="100" Margin="5"/>
            <Button Content="Отмена" Style="{StaticResource PrimaryButton}" Click="OnCancelClick" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Page>
