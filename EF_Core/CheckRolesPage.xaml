<Page x:Class="EF_Core.CheckRolesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EF_Core"
      mc:Ignorable="d" 
      Background="White"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CheckRolesPage">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="Назад" Click="BackBtn_Click"/>
        </StackPanel>

        <ComboBox Grid.Row="1"
                  Name="RoleComboBox"
                  Style="{StaticResource PrimaryComboBox}"
                  ItemsSource="{Binding Roles}"
                  DisplayMemberPath="Title"
                  SelectedValuePath="Id"
                  SelectedValue="{Binding SelectedRoleId, UpdateSourceTrigger=PropertyChanged}"
                  Margin="0,0,0,10"/>

        <ListView Grid.Row="2"
                  ItemsSource="{Binding UsersByRole}"
                  BorderBrush="LightGray"
                  BorderThickness="1"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{Binding Login}" Width="120" FontWeight="Medium"/>
                        <TextBlock Text=" — " Foreground="DarkGray"/>
                        <TextBlock Text="{Binding Role.Title}" Foreground="Blue"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
